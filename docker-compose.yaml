version: '3.8'

services: 
      
  app:
  #  build:
  #    context: .
  #    dockerfile: ./Dockerfile.cloud
    image: igogo/devops-learning-warsaw:v0.0.3
    ports:
      - 80:80
    stdin_open: true
    tty: true
